"use client"

import React, { Suspense } from "react";
import EventsList from "./EventsList";

const page = () => {
  return (
    <div>
        <EventsList />      
    </div>
  );
};

export default page;
